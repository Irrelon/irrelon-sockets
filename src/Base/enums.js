const DISCONNECTED = 0;
const CONNECTING = 1;
const CONNECTED = 2;
const STARTED = 3;
const STOPPED = 4;
const CLIENT = 5;
const SERVER = 6;
const COMMAND = "command";

module.exports = {
	DISCONNECTED,
	CONNECTING,
	CONNECTED,
	STARTED,
	STOPPED,
	CLIENT,
	SERVER,
	COMMAND
};