const DISCONNECTED = 0;
const CONNECTING = 1;
const CONNECTED = 2;
const READY = 3;
const STARTED = 4;
const STOPPED = 5;
const CLIENT = 6;
const SERVER = 7;
const COMMAND = "command";

module.exports = {
	DISCONNECTED,
	CONNECTING,
	CONNECTED,
	READY,
	STARTED,
	STOPPED,
	CLIENT,
	SERVER,
	COMMAND
};